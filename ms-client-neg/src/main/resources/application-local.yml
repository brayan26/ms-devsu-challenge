#Logger
logging:
  level:
    org:
      springframework:
        web: ERROR
        boot:
          autoconfigure:
            web: ERROR
#      hibernate:
#        SQL: debug
spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 1122q!
    driver-class-name: com.mysql.cj.jdbc.Driver

kafka:
  topic: ms.client.neg.client.event
  server: localhost:9092
  #Producers
  producer:
    key: ms-client-neg
    retries: 3
    acks: all
    max:
      flight:
        request: 5
    enable:
      idempotence: true
    batch:
      size: 16384
    buffer:
      memory: 33554432
    linger:
      ms: 10